# 원본 사진·영상 (532MB) — git 대신 rsync 로 옮긴다
staging/
organized/
review/
trash/
dist/

# 스캔 캐시 / 이동 기록 — 언제든 다시 만들 수 있다
data/meta.json
data/move_manifest.jsonl

# 손으로 쓴 요약·메모·장소·생일·영상 태그(data/*.json)와 갤러리(index.html, LOG.md,
# gallery/thumbs 5.6MB)는 일부러 추적한다. 원본 없이도 페이지가 보이도록.

.DS_Store
__pycache__/
*.pyc
scripts/.jiho.py.theme-backup
